---
apiVersion: v1
kind: Service
metadata:
  name: http-echo-nodeport
  namespace: demo
spec:
  type: NodePort
  selector:
    app: http-echo
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080

---
apiVersion: v1
kind: Service
metadata:
  name: http-echo-clusterip
  namespace: demo
spec:
  type: ClusterIP
  selector:
    app: http-echo
  ports:
    - name: http
      port: 80
      targetPort: 80
